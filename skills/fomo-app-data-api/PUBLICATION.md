# Publication record

Prepared for ClawHub as `fomo-app-data-api`, display title **Fomo App Crypto Trading API**, version `1.0.1`.

Public description: Read-only FOMO App Data API for agent research and market
data across Solana, meme coins trading, crypto, social trading, and on-chain
activity; it provides no investing advice or transaction capabilities.

Publish the `skills/fomo-app-data-api` folder as the package. ClawHub excludes
the local `skill-card.md` and generates skill cards separately; all other
package files are intended to be published and validated from this folder.

The registry is the source of truth for publication and moderation status.
This file does not claim publication, moderation approval, ratings, or live
availability until verified with `clawhub inspect --json`.

Publication metadata must identify the source repository, reviewed commit, git
ref, and package path. Publish only from a clean extracted archive.
