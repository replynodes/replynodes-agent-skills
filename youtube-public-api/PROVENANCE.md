# Provenance

This package is maintained in `replynodes/replynodes-agent-skills`.

The route contract is cross-checked against the ReplyNodes Fetcher YouTube gateway implementation (`services/replynodes-fetcher/internal/gateway/providerops.go`) and the deployed capability endpoint. Generated/package output must be regenerated from its canonical source when a generator is introduced; this release contains hand-authored contract documentation only.
